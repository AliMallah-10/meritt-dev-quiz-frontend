query GetProduct($id: ObjectID!) {
  getProduct(_id: $id) {
    item {
      _id
      title
      description
      quantity
      images
      createdAt
    }
    error {
      field
      message
    }
  }
}
